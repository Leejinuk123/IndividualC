> ## 1. 포인터로 주소 지시하기
```c++
#include<stdio.h>
int main(){
	int a=3;
	int *p;
	p=&a; 
	printf("%d\n",*p); //*p는 *p가 지시하는 주소 안의 값을 의미 
	
	printf("%p\n",&a); //%p는 &a의 주소 자체를 보여준다.
	printf("%p\n",p); //=printf("%p\n",&a);
	printf("%d\n",*p); //=printf("%d\n",a);
}
```
